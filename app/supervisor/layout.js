export const metadata = {
  title: 'Supervisor Portal | RhockDeal',
  description: 'Regional management node for RhockDeal ecosystem.',
};

export default function SupervisorLayout({ children }) {
  return children;
}
